# C02 — Strings
## Piscine C

---

### Chapter I: Instructions

* Only the function `write` is allowed unless stated otherwise.
* Compilation must succeed with `gcc -Wall -Wextra -Werror`.
* The Norm must be respected at all times.

---

### Chapter II: Mandatory Part

#### Exercise 00: ft_strcpy
* **Turn-in directory:** `ex00/`
* **Files to turn in:** `ft_strcpy.c`
* **Allowed functions:** None

Reproduce the behavior of the function `strcpy` (`man strcpy`).
```c
char *ft_strcpy(char *dest, char *src);
```

---

#### Exercise 01: ft_strncpy
* **Turn-in directory:** `ex01/`
* **Files to turn in:** `ft_strncpy.c`
* **Allowed functions:** None

Reproduce the behavior of the function `strncpy` (`man strncpy`).
```c
char *ft_strncpy(char *dest, char *src, unsigned int n);
```

> **Trap:** If `src` is shorter than `n`, pad the rest of `dest` with `'\0'`.

---

#### Exercise 02: ft_str_is_alpha
* **Turn-in directory:** `ex02/`
* **Files to turn in:** `ft_str_is_alpha.c`
* **Allowed functions:** None

Create a function that returns `1` if the string given as parameter contains only alphabetical characters, and `0` otherwise. An empty string returns `1`.
```c
int ft_str_is_alpha(char *str);
```

---

#### Exercise 03: ft_str_is_numeric
* **Turn-in directory:** `ex03/`
* **Files to turn in:** `ft_str_is_numeric.c`
* **Allowed functions:** None

Create a function that returns `1` if the string given as parameter contains only digits, and `0` otherwise.
```c
int ft_str_is_numeric(char *str);
```

---

#### Exercise 04: ft_str_is_lowercase
* **Turn-in directory:** `ex04/`
* **Files to turn in:** `ft_str_is_lowercase.c`
* **Allowed functions:** None

Create a function that returns `1` if the string given as parameter contains only lowercase alphabetical characters, and `0` otherwise.
```c
int ft_str_is_lowercase(char *str);
```

---

#### Exercise 05: ft_str_is_uppercase
* **Turn-in directory:** `ex05/`
* **Files to turn in:** `ft_str_is_uppercase.c`
* **Allowed functions:** None

Create a function that returns `1` if the string given as parameter contains only uppercase alphabetical characters, and `0` otherwise.
```c
int ft_str_is_uppercase(char *str);
```

---

#### Exercise 06: ft_str_is_printable
* **Turn-in directory:** `ex06/`
* **Files to turn in:** `ft_str_is_printable.c`
* **Allowed functions:** None

Create a function that returns `1` if the string given as parameter contains only printable characters (ASCII 32–126), and `0` otherwise.
```c
int ft_str_is_printable(char *str);
```

---

#### Exercise 07: ft_strupcase
* **Turn-in directory:** `ex07/`
* **Files to turn in:** `ft_strupcase.c`
* **Allowed functions:** None

Create a function that transforms every lowercase character to uppercase.
```c
char *ft_strupcase(char *str);
```

---

#### Exercise 08: ft_strlowcase
* **Turn-in directory:** `ex08/`
* **Files to turn in:** `ft_strlowcase.c`
* **Allowed functions:** None

Create a function that transforms every uppercase character to lowercase.
```c
char *ft_strlowcase(char *str);
```

---

#### Exercise 09: ft_strcapitalize
* **Turn-in directory:** `ex09/`
* **Files to turn in:** `ft_strcapitalize.c`
* **Allowed functions:** None

Create a function that capitalizes the first letter of each word and lowercases the rest. A word is a sequence of alphanumeric characters.
```c
char *ft_strcapitalize(char *str);
```

> **Trap:** "hello, WORLD! 42is cool" → "Hello, World! 42is Cool". Numbers don't count as word separators internally, but they don't get capitalized.

---

#### Exercise 10: ft_strlcpy
* **Turn-in directory:** `ex10/`
* **Files to turn in:** `ft_strlcpy.c`
* **Allowed functions:** None

Reproduce the behavior of the function `strlcpy` (`man strlcpy`). Returns the length of `src`.
```c
unsigned int ft_strlcpy(char *dest, char *src, unsigned int size);
```
